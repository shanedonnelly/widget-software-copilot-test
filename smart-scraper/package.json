{
  "name": "scraper-script",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/scraper.js --bundle --format=iife --outfile=dist/scraper.js --minify",
    "build:dev": "esbuild src/scraper.js --bundle --format=iife --outfile=dist/scraper.js",
    "serve": "npx http-server ./dist --cors -p 9004"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "dom-to-semantic-markdown": "^1.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
